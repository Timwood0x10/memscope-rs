name: memscope-rs CI

on:
  push:
    branches: [master, dev]
    tags: ["v*"]
  pull_request:
    branches: [master]

# Permissions for CI operations
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTFLAGS: "-D warnings"

jobs:
  quality:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Verify toolchain
        run: |
          rustc --version
          cargo --version
          rustfmt --version

      - name: Handle Cargo.lock compatibility
        shell: bash
        run: |
          # Always regenerate Cargo.lock for CI to ensure compatibility
          echo "Regenerating Cargo.lock for CI compatibility..."
          rm -f Cargo.lock
          cargo generate-lockfile
          echo "Generated compatible Cargo.lock version: $(head -n1 Cargo.lock)"

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: quality-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy analysis
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Check compilation
        run: cargo check --workspace --all-targets --all-features

  test:
    name: Tests
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust: [stable]
        include:
          - os: ubuntu-latest
            rust: nightly
            experimental: true

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust ${{ matrix.rust }}
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}

      - name: Handle Cargo.lock compatibility
        shell: bash
        run: |
          # Always regenerate Cargo.lock for CI to ensure compatibility
          echo "Regenerating Cargo.lock for CI compatibility..."
          rm -f Cargo.lock
          cargo generate-lockfile
          echo "Generated compatible Cargo.lock version: $(head -n1 Cargo.lock)"

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: test-${{ matrix.os }}-${{ matrix.rust }}-${{ hashFiles('**/Cargo.lock') }}

      - name: Run library tests (auto-signal + periodic features)
        run: cargo nextest run --workspace --features auto-signal,periodic -E 'kind(lib)'
        continue-on-error: ${{ matrix.experimental == true }}

      - name: Run integration tests (auto-signal + periodic features)
        run: cargo nextest run --workspace --features auto-signal,periodic -E 'kind(test)'
        continue-on-error: ${{ matrix.experimental == true }}

      - name: Test examples
        run: |
          cargo build --examples --features auto-signal,periodic
        continue-on-error: ${{ matrix.experimental == true }}

  benchmarks:
    name: Benchmarks
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Handle Cargo.lock compatibility
        shell: bash
        run: |
          # Always regenerate Cargo.lock for CI to ensure compatibility
          echo "Regenerating Cargo.lock for CI compatibility..."
          rm -f Cargo.lock
          cargo generate-lockfile
          echo "Generated compatible Cargo.lock version: $(head -n1 Cargo.lock)"

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: bench-${{ hashFiles('**/Cargo.lock') }}

      - name: Run benchmarks
        run: cargo bench --bench minimal_performance

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: target/criterion/

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Handle Cargo.lock compatibility
        shell: bash
        run: |
          # Always regenerate Cargo.lock for CI to ensure compatibility
          echo "Regenerating Cargo.lock for CI compatibility..."
          rm -f Cargo.lock
          cargo generate-lockfile
          echo "Generated compatible Cargo.lock version: $(head -n1 Cargo.lock)"

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: docs-${{ hashFiles('**/Cargo.lock') }}

      - name: Build documentation
        run: cargo doc --workspace --all-features --no-deps

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Handle Cargo.lock compatibility
        shell: bash
        run: |
          # Always regenerate Cargo.lock for CI to ensure compatibility
          echo "Regenerating Cargo.lock for CI compatibility..."
          rm -f Cargo.lock
          cargo generate-lockfile
          echo "Generated compatible Cargo.lock version: $(head -n1 Cargo.lock)"

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Run security audit
        run: cargo audit || echo "Audit check completed (some warnings may be acceptable)"

      - name: Check licenses
        run: |
          cargo install cargo-license
          cargo license --json | jq -r '.[] | select(.license != "MIT" and .license != "Apache-2.0" and .license != "BSD-3-Clause" and .license != "BSD-2-Clause") | .name + ": " + .license'

  coverage:
    name: Code Coverage
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: coverage-${{ matrix.os }}-${{ hashFiles('**/Cargo.lock') }}

      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov

      - name: Generate coverage
        run: cargo llvm-cov --workspace --lcov --output-path lcov.info

      - name: Upload to codecov
        uses: codecov/codecov-action@v3
        with:
          files: lcov.info
          fail_ci_if_error: false

